# Assets

Add `og-default.png` here (1200×630 px) for default Open Graph and Twitter Card images. The site references `/zero-gtm/assets/og-default.png` in `_config.yml`. Excluded from Jekyll build via `exclude` if needed; otherwise leave this README and add the image.
